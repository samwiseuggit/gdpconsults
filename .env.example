# SMTP Configuration (for email sending via Mailpit or real SMTP)
SMTP_HOST=mailpit
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=

# Email addresses
FROM_EMAIL=noreply@gdpconsults.ca
TO_EMAIL=info@gdpconsults.ca

# Frontend URL (used for CORS)
FRONTEND_URL=https://yourdomain.com
